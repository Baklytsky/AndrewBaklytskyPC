name: Deploy /dist to carbon-dist

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - '.vscode/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        always-auth: true
        registry-url: https://npm.pkg.github.com/
    - name: Get yarn cache directory path
      id: yarn-cache-dir-path
      run: echo "dir=$(yarn cache dir)" >> $GITHUB_OUTPUT
    - uses: actions/cache@v4
      id: yarn-cache
      with:
        path: |
          ${{ steps.yarn-cache-dir-path.outputs.dir }}
          'node_modules'
        key: ${{ runner.os }}-yarn-v3-${{ hashFiles('**/yarn.lock') }}
        restore-keys: |
          ${{ runner.os }}-yarn-v3-
    - run: yarn install
      env:
        NODE_AUTH_TOKEN: ${{secrets.ORG_PKG_READ}}
    - name: "Build assets"
      run: yarn build:assets

    - name: Set up SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan github.com >> ~/.ssh/known_hosts
        pwd

    - name: Remove existing carbon-dist folder if it exists
      run: |
        if [ -d "carbon-dist" ]; then
          rm -rf carbon-dist
        fi

    - name: Clone target repository
      run: |
        cd ~/
        git clone git@github.com:invisiblethemes/carbon-dist.git carbon-dist

    - name: Sync /dist contents to the root of carbon-dist
      run: |
        # Ensure the dist folder exists
        if [ -d ~/work/carbon/carbon/dist ]; then
          rsync -av --delete --exclude '.git/' ~/work/carbon/carbon/dist/ ~/carbon-dist/
        else
          echo "No dist folder found, skipping sync."
          exit 1
        fi
        # Commit and push changes
        cd ~/carbon-dist
        git remote add dist https://x-access-token:${{ secrets.GH_PAT }}@github.com/invisiblethemes/carbon-dist.git
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .
        git commit -m "Update files from /dist (commit $GITHUB_SHA)" || echo "No changes to commit"
        git push origin main