{
  "name": "true",
  "version": "1.0.0",
  "license": "Copyright Presidio Creative",
  "scripts": {
    "dev": "shopify theme dev -e production",
    "deploy": "shopify theme push -e production",
    "pull": "shopify theme pull -e production",
    "pull:settings": "shopify theme pull -e production --only='config/settings_data.json' --only='templates/' --only='sections/*.json'",
    "login-store": "shopify theme open -s true-linkswear.myshopify.com"
  },
  "devDependencies": {},
  "dependencies": {}
}
