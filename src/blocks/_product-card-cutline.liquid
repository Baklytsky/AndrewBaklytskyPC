{% liquid
  assign product = closest.product
-%}

{% render 'product-card-cutline', product: product, shopify_attributes: block.shopify_attributes %}

{% schema %}
{
  "name": "Product card cutline",
  "tag": null,
  "settings": [],
  "presets": [
    {
      "name": "Product card cutline",
      "category": "Product"
    }
  ]
}
{% endschema %}
