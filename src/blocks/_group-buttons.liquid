<div
  class="hero__button-group"
  data-aos="hero"
  data-aos-order="auto"
  data-aos-anchor="#{{ section.id }}"
  {{ block.shopify_attributes }}
>
  {% content_for 'blocks' %}
</div>

{% schema %}
{
  "name": "Group buttons",
  "tag": null,
  "blocks": [{"type": "button"}],
  "presets": [
    {
      "name": "Group buttons",
      "blocks": [
        {
          "type": "button",
          "settings": {
            "text": "View products",
            "type": "btn--primary"
          }
        },
        {
          "type": "button",
          "settings": {
            "text": "Shop sale",
            "type": "btn--secondary"
          }
        }
      ]
    }
  ]
}
{% endschema %}
