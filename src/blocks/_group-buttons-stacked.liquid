<div
  class="hero__button-group hero__button-group--stacked"
  data-aos="hero"
  data-aos-order="auto"
  data-aos-anchor="#{{ section.id }}"
  {{ block.shopify_attributes }}
>
  {% content_for 'blocks' %}
</div>

{% schema %}
{
  "name": "Stacked buttons",
  "tag": null,
  "blocks": [{"type": "button"}],
  "presets": [
    {
      "name": "Stacked buttons",
      "blocks": [
        {
          "type": "button",
          "settings": {
            "text": "View products",
            "type": "btn--primary"
          }
        },
        {
          "type": "button",
          "settings": {
            "text": "Shop sale",
            "type": "btn--secondary"
          }
        }
      ]
    }
  ]
}
{% endschema %}
